{
    "buildCommand": "npm install && npm run typecheck",
    "env": {
        "NODE_ENV": "production"
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}